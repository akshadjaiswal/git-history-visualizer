# Environment Variables

# GitHub Personal Access Token (optional)
# Get your token from: https://github.com/settings/tokens
# Required scopes: public_repo (read-only access to public repositories)
# Without this token, rate limit is 60 requests/hour
# With this token, rate limit increases to 5,000 requests/hour

# IMPORTANT: Server-side only - token is never exposed to the browser
# This token is used by the /api/github endpoint to authenticate with GitHub API
GITHUB_TOKEN=ghp_your_token_here

